import {View, Text} from 'react-native';
import React from 'react';
import MypageSubscribeFeedList from '../components/MypageSubscribeFeedList';

type Props = {};

const MypageSubscribeFeedListContainer = (props: Props) => {
  return <MypageSubscribeFeedList />;
};

export default MypageSubscribeFeedListContainer;
